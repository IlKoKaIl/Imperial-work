# Superseded

These three files were an earlier, fragmented attempt at the same job.
All their content is consolidated into `PROJECT_RECORD.md` at the top of
`fir_validation_aug2026/`. Kept only so nothing is lost; safe to delete.
